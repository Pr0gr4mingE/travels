import 'package:flutter/material.dart';

class LabelTitulo extends StatelessWidget{
  const LabelTitulo ({super.key});
    final String nome;
  @override
  Widget build (BuildContext context){
    return TextStyle(
      fontFamily: 
      );
    
  }
}